{
	"id": "playwright-chromium",
	"version": "0.1.0",
	"name": "Playwright + Chromium",
	"description": "Bakes Chromium in rather than leaving it to be fetched on demand: a running sandbox's deny-by-default egress allowlist has no Playwright CDN entry, so a session's own `playwright install` would hang there.",
	"options": {
		"version": {
			"type": "string",
			"default": "1.62.0",
			"description": "Playwright version."
		}
	},
	"containerEnv": {
		"PLAYWRIGHT_BROWSERS_PATH": "/ms-playwright"
	},
	"installsAfter": [
		"ghcr.io/devcontainers/features/node",
		"ghcr.io/devcontainers/features/common-utils"
	]
}
