{
	"name": "cyrus-router-protocol",
	"version": "0.2.66",
	"description": "Wire frame schemas and RPC method allowlist for Cyrus Router per-user device routing",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/cyrusagents/cyrus.git",
		"directory": "packages/router-protocol"
	},
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsgo",
		"dev": "tsgo --watch",
		"test": "vitest",
		"test:run": "vitest run",
		"typecheck": "tsgo --noEmit"
	},
	"dependencies": {
		"zod": "4.3.6"
	},
	"devDependencies": {
		"@types/node": "^20.19.43",
		"@typescript/native-preview": "7.0.0-dev.20260707.2",
		"vitest": "^4.1.10"
	},
	"publishConfig": {
		"access": "public"
	}
}
