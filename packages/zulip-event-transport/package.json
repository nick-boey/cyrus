{
	"name": "cyrus-zulip-event-transport",
	"version": "0.2.71",
	"description": "Zulip event transport for receiving and verifying Zulip outgoing webhooks",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/cyrusagents/cyrus.git",
		"directory": "packages/zulip-event-transport"
	},
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"typecheck": "tsc --noEmit",
		"test": "vitest",
		"test:run": "vitest run"
	},
	"dependencies": {
		"cyrus-core": "workspace:*",
		"fastify": "^5.12.1"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.3.3",
		"vitest": "^4.1.10"
	},
	"publishConfig": {
		"access": "public"
	}
}
